# Google Sheets & Auth
gspread==6.1.0
google-auth==2.29.0

# API & Networking
requests==2.31.0

# Optional: OpenAI (The script checks if this is available)
openai==1.14.3

# Standard processing (usually pre-installed, but good for safety)
typing-extensions
